Before the first ClickHouse query on a table, inspect available databases, tables, and columns.

Use the real database-qualified table name from `describeConnection`. Do not infer table or column names from the in-app sheet alias.
