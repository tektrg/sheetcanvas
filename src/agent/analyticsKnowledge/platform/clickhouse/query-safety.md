For ClickHouse, resolve relative dates to absolute dates from `context.dateAnchors`.

Use half-open date ranges for periods, require `GROUP BY` plus `LIMIT` for chartable result sets, and run atomic cross-checks for complex queries.
