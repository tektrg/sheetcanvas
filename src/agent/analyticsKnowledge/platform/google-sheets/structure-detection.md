For Google Sheets data, verify header rows, date columns, numeric columns, blank rows, and mixed types before aggregating.

If range choice affects the result, record the chosen range in the query brief scope.
