For Google Analytics, call `listConnectionProperties` and `describeConnection` before reporting.

Use only dimensions and metrics returned by the connector, preserve GA native relative date syntax, and call out metric scope or sampling caveats when relevant.
